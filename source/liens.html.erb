---
title: Liens
---

<% if data.links.categories %>
  <% data.links.categories.each do |cat| %>
    <p><%= cat.label %></p>
    <ul>
      <% cat.items.each do |item| %>
        <li><%= link_to item.label, item.href %></li>
      <% end %>
    </ul>
  <% end %>
<% end %>
