.col-md-3.col-lg-3.hidden-xs.sidebar.sidebar-left
  %ul#menu-submenu.menu
    %li{nav_active('/articles')}
      = link_to "Tous les articles", '/articles'
    - blog.tags.each do |tag, articles|
      %li{nav_active(tag_path(tag))}
        = link_to "#{tag} (#{articles.size})", tag_path(tag)
